diff -purN patched/gps_test.c source/gps_test.c
--- a/source/gps_test.c	2019-10-03 20:18:36.600334126 +0300
+++ b/source/gps_test.c	2019-10-03 20:11:23.613022513 +0300
@@ -81,7 +81,7 @@ int main(int argv, char **argc)
         /* Put this in a loop with a call to a high resolution sleep () in it. */
         if (gps_waiting(&GpsData, 500)) {
             //errno = 0;
-            if (gps_read(&GpsData) == -1) {
+            if (gps_read(&GpsData, NULL, 0) == -1) {
                 dprintf(1, "Read error\n");
                 //exit(1);
                 sleep(1);
