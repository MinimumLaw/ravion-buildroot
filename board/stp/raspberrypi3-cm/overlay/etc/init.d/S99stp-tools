#!/bin/sh
#
# Start stp-tools
#

start() {
	printf "Starting stp-tools: "
	/usr/local/bin/sc_uart >/dev/null 2>/var/log/sc_uart.err &
	echo "OK"
}

stop() {
	printf "Stopping stp-tools: "
	killall /usr/local/bin/sc_uart
	echo "OK"
}

case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	stop
	start
	;;
  *)
	echo "Usage: $0 {start|stop|restart|reload}"
	exit 1
esac

exit $?
